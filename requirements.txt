# requirements.txt — versi di-PIN (Audit R1#8 / R2 versioning).
# Ditujukan untuk Kaggle Notebook (Python 3.10/3.11+, 2x GPU T4).
# Catatan chemprop: versi 1.x (mis. 1.7.1) TIDAK tersedia untuk Python modern — rilis 1.x
# terakhir (1.6.1) mensyaratkan Python <3.9. src/models/dmpnn_model.py memanggil CLI
# `chemprop train` / `chemprop predict` (chemprop 2.x, https://chemprop.readthedocs.io).

numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.4.2

# Deep learning stack
torch==2.2.1
transformers==4.40.2
tokenizers==0.19.1

# Chemistry
# Catatan: deepchem TIDAK dipakai lagi — src/data_loader.py mengunduh CSV mentah
# MoleculeNet langsung (bypass featurizer DeepChem yang crash pada SMILES invalid).
rdkit==2023.9.6
chemprop>=2.1,<3.0

# Utilities
tqdm==4.66.4
matplotlib==3.8.4        # untuk 06_make_plots.py (opsional)

# Python: 3.10.x (Kaggle default). Dicantumkan di Metodologi/Lampiran paper.
